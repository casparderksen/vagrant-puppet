---
version: 5

docker_build_dir: &docker_build_dir     /var/tmp/docker
docker_secrets_dir: &docker_secrets_dir /etc/docker-secrets


profiles::docker::build::build_dir:   *docker_build_dir
profiles::docker::stack::build_dir:   *docker_build_dir
profiles::docker::stack::secrets_dir: *docker_secrets_dir
